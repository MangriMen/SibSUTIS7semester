<Page
    x:Class="lab1.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid
        x:Name="ContentArea"
        Margin="0,0,0,45"
        RowSpacing="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions />

        <Grid
            Grid.Row="0"
            ColumnSpacing="16"
            RowSpacing="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Row="0"
                Grid.Column="0"
                Text="Входные данные" />
            <ScrollViewer Grid.Row="1" Grid.Column="0">
                <TextBox
                    x:Name="ManualInput"
                    MaxHeight="200"
                    AcceptsReturn="True"
                    PlaceholderText="Введите грамматику"
                    Text="{x:Bind RawGrammar}"
                    TextChanged="ManualInput_TextChanged"
                    TextWrapping="Wrap" />
            </ScrollViewer>
            <StackPanel
                Grid.Row="2"
                Grid.Column="0"
                Orientation="Horizontal"
                Spacing="8">
                <TextBlock VerticalAlignment="Center" Text="Длина цепочки" />
                <TextBox
                    LosingFocus="SequenceLength_LostFocus"
                    Text="{x:Bind SequenceLength, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    TextAlignment="Center"
                    TextChanged="SequenceLength_TextChanged"
                    TextChanging="SequenceLength_TextChanging" />
            </StackPanel>
            <Button
                Grid.Row="3"
                Grid.Column="0"
                Click="StartClick">
                Начать
            </Button>
        </Grid>
        <Grid Grid.Row="1" RowSpacing="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Цепочки" />
            <ScrollViewer Grid.Row="1">
                <TextBox
                    x:Name="Output"
                    AcceptsReturn="True"
                    IsReadOnly="True"
                    TextWrapping="Wrap" />
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
