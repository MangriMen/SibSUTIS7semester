<Page
    x:Class="lab3.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:community="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d">

    <Grid x:Name="ContentArea" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Spacing="8">
            <TextBlock Text="Грамматика"/>
            <TextBox Text="{x:Bind Grammar, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>
        <Button>Парсинг</Button>
        <community:DataGrid Grid.Row="1" ItemsSource="{x:Bind Edges, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
        </community:DataGrid>
    </Grid>
</Page>
